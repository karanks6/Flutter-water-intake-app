<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core notification permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Exact alarm permissions (Android 12+ requires one of these) -->
    <!-- USE_EXACT_ALARM: auto-granted on most devices (API 33+) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- SCHEDULE_EXACT_ALARM: user-grantable fallback for devices that restrict USE_EXACT_ALARM -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="AQUA LOG"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding metadata -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: scheduled notification receiver -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- flutter_local_notifications: boot receiver to reschedule notifications after reboot -->
        <!-- Covers all major Android OEM boot events for cross-device compatibility -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="true">
            <intent-filter>
                <!-- Standard Android boot -->
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <!-- App update / reinstall -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.PACKAGE_REPLACED"/>

                <!-- Xiaomi / MIUI / HyperOS quick boot -->
                <action android:name="android.quickboot.poweron"/>
                <!-- HTC quick boot -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
                <!-- Huawei quick boot -->
                <action android:name="com.huawei.action.BOOT_COMPLETED"/>
                <!-- Samsung (older devices sometimes use REBOOT) -->
                <action android:name="android.intent.action.REBOOT"/>
                <!-- OnePlus / OxygenOS -->
                <action android:name="com.oneplus.action.BOOT_COMPLETED"/>
                <!-- Oppo / ColorOS -->
                <action android:name="com.coloros.mcs.action.INIT_NOTIFICATION"/>
                <!-- Vivo / OriginOS -->
                <action android:name="com.vivo.action.BOOT_COMPLETED"/>
                <!-- Meizu / Flyme -->
                <action android:name="com.meizu.action.BOOT_COMPLETED"/>
                <!-- Asus / ZenUI -->
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- flutter_local_notifications: action button receiver -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

    </application>

</manifest>
